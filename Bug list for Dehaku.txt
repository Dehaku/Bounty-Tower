Fixed!-AI still ignores walls sometimes. :/

Outdated-If a human (which is holding something) is killed, the zombie critter for it will wander around instead of hunting humans.

Fixed!-The Shift-tab spawn menu is unusable since shift-click now spawns a missile(which kills whatever you spawn).

Fixed!-The game looks for City.bmp instead of City.png

Fixed!-The game looks for Sky.bmp not Sky.png

Fixed!-The game looks for Grass.bmp, not Grass.png

Nope-Add a config file so you can edit file names without compiling?